import { $ } from "../index";
export default class {
    private readonly $;
    constructor(globalData: $);
    mapPlugins(): void;
    mapPlugin(path: any): void;
    applyPlugin(mapComponent: any): void;
    parsePagePaths(paths: any, callback: any, reject: any): void;
}
