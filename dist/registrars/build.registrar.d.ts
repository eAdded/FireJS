import { $ } from "../index";
import MapComponent from "../classes/MapComponent";
export default class {
    private readonly $;
    constructor(globalData: $);
    registerForSemiBuild(mapComponent: MapComponent): Promise<unknown>;
}
